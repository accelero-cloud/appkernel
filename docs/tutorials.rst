Tutorials
=========

The best place to start is the :ref:`Introduction` section, which walks through
building a complete service step by step — model, repository, REST endpoints,
and lifecycle hooks.

For specific topics, jump directly to:

* :ref:`Installation` — get up and running in under two minutes
* :ref:`The Model Class` — field metadata, validation, generators, converters, and marshallers
* :ref:`Repositories` — CRUD, query DSL, indexes, optimistic locking
* :ref:`Services` — REST endpoints, filtering, pagination, custom actions, HATEOAS
* :ref:`Role Based Access Management` — JWT tokens, RBAC, CORS, CSRF
* :ref:`API Versioning` — URL versioning, view models, deprecation
* :ref:`I18n (Internationalisation)` — Babel integration, translation workflow
* `File Storage <file_storage>`_ — filesystem and GridFS backends, validation chain
* `Transparent REST Client Proxies <clients>`_ — inter-service HTTP calls, circuit breaker, file upload/download
* `Rate Limiting <rate_limiting>`_ — per-IP throttling, per-endpoint limits
* `OpenAPI / Swagger <openapi>`_ — auto-generated API documentation
